import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.ctfassets.net', 'assets.entrepreneur.com', 'encrypted-tbn0.gstatic.com', 'via.placeholder.com', 'emprendedores.es', '*', 'static.vecteezy.com'], 
    
  },
};

export default nextConfig;
